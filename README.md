# 🌌 Star Wars Graph Explorer

React + TypeScript тестове завдання.
Веб-додаток дозволяє переглядати список героїв Star Wars та візуалізувати зв’язки між героями, фільмами та зоряними кораблями у вигляді графа.

---

## 🚀 Основні технології

- **React 19 + TypeScript**  
- **React Flow** — побудова графів  
- **Vite** — швидка збірка та дев-сервер  
- **Vitest + Testing Library** — юніт-тести

---

## 📦 Встановлення та запуск

```bash
# Клонування репозиторію
git clone https://github.com/oborysyu/star-wars-graph.git
cd star-wars-graph

# Встановити залежності
npm install

# Запустити у режимі розробки
npm run dev

# Запустити тести
npm run test
```

Після запуску dev-сервера застосунок буде доступний за адресою:
👉 [http://localhost:5173](http://localhost:5173)

---

## 📁 Структура проекту

```
star-wars-graph/
├── src/
│   ├── api.ts               # Робота з API
│   ├── App.tsx              # Головний компонент
│   ├── main.tsx             # Точка входу
│   ├── types.ts             # Типи
│   ├── components/
│   │   ├── HeroList.tsx     # Список героїв з infinite scroll
│   │   └── HeroGraph.tsx    # Побудова графа через React Flow
│   └── __tests__/
│       ├── HeroList.test.tsx
│       └── HeroGraph.test.tsx
├── vite.config.ts
├── vitest.setup.ts
├── package.json
├── tsconfig.json
└── README.md
```

---

## 🌠 Як працює додаток

1. **HeroList** робить запит до API `/people?page=` та показує героїв з infinite scroll.  
2. При кліку на героя — передає його до **HeroGraph**.  
3. **HeroGraph** завантажує фільми (`hero.films`) і зорельоти (`film.starships`) та будує граф:
   - центральна нода — герой,
   - зв’язки до фільмів,
   - від фільмів — до кораблів.

---

## 🧪 Тестування

Тести знаходяться у `src/__tests__/` і перевіряють:
- відображення списку героїв і виклик `onSelect` у HeroList
- побудову графа у HeroGraph (моки fetch)

Запуск:
```bash
npm run test
```

---

## 💡 Можливі покращення

- кешування запитів у пам’яті (memoization)
- фільтри та пошук героїв
- кастомні ноди у React Flow з tooltip-ами
- показ зображень фільмів або кораблів
- адаптивний дизайн (Tailwind або shadcn/ui)

---

## 🔗 API

Використовується публічне API:  
[https://sw-api.starnavi.io](https://sw-api.starnavi.io)

API:
- `/people?page=` — герої
- `/films/:id` — фільми
- `/starships/:id` — кораблі

---
📅 2025